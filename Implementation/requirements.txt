# Core Libraries
streamlit>=1.29.0
plotly>=5.14.1
pandas>=2.0.1
requests>=2.28.2

# Database and Async
aiosqlite>=0.19.0

# Cerebras SDK (specify the correct installation method)
# You might need to install this separately via their specific instructions
# cerebras-cloud-sdk

# Optional Visualization
matplotlib>=3.7.1
seaborn>=0.12.2

# Development and Type Checking
typing_extensions>=4.5.0

# Note: sqlite3 is a built-in Python standard library, do not specify version
